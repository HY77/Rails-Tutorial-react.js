<% provide(:title, "All users") %>
<h1>Users</h1>

<div class="row">
  <div class="search_form">
    <%= render "search_form" %>
  </div>
</div>

<%= will_paginate @users %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate @users %>